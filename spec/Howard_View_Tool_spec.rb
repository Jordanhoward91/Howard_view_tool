require "spec_helper"

describe HowardViewTool do
  it "has a version number" do
    expect(HowardViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
